# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'internAppTask' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
pod 'TwitterKit'
pod 'SQLite.swift'
  target 'internAppTaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'internAppTaskUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
